apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
spec:
  type: {{ .Values.heron.service.type }}
  ports:
    - port: {{ .Values.heron.service.port }}
      targetPort: {{ .Values.heron.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}
