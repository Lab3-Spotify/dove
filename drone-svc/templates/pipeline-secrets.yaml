{{- range $name, $value := .Values.drone.pipelineSecrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}
  namespace: {{ $.Release.Namespace }}
type: Opaque
stringData:
  value: {{ $value | quote }}
---
{{- end }}
